# Arquivo de pastas anteriores

Esta pasta preserva as antigas estruturas `03-*`, `04-*`, `Campanha_Consorcio` e `xx-*`, inclusive versões alternativas, arquivos temporários e materiais técnicos. O conteúdo de uso corrente está nas pastas `CON-` e `EST-` na raiz do projeto. Não atualize duas cópias do mesmo material em paralelo.